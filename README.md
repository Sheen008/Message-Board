# Message Board

Simple internal message board system.

## Structure

- `client` - user submission page
- `screen` - big screen display
- `admin` - management page
- `server` - Node.js API server

## Usage

```
cd server
npm install
npm start
```

Visit `/client`, `/screen`, or `/admin` in your browser.
